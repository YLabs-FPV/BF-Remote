{
  "name": "bf-remote",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "dev": "pnpm -C web build && pnpm -C server dev",
    "build": "pnpm -C web build",
    "deploy": "pnpm -C web build && pnpm -C server run deploy",
    "typecheck": "pnpm -C shared typecheck && pnpm -C server typecheck && pnpm -C web check"
  }
}
